<% include partials/_header %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-7">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <td>n°</td>
            <td>Name</td>
            <td>Actions</td>
          </tr>
        </thead>
        <tbody>
            <h1>Cart</h1>
            <% for(var i = 0; i < dataJson.length; i++) { %>
              <tr>
                <td><%= dataJson[i].id %></td>
                <td>Carrito</td>
                <td>
                  <a href="/cartview/<%= dataJson[i].id %>" class="btn btn-info border rounded">view</a>
                  <form action="/delcart/<%= dataJson[i].id %>?_method=DELETE" method="POST">
                    <button type="submit" class="btn btn-danger border rounded">Delete</button>
                  </form>
                </td>
              </tr>
            <% } %>
        </tbody>
      </table>
    </div>
    
  </div>
  <form action="/delete?_method=DELETE" method="POST">
    <button type="submit" class="btn btn-primary border" >
      delete all
    </button>
  </form>
</div>

<% include partials/_footer %>
